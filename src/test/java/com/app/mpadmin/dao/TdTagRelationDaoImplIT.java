/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/dao/DaoImplIT.e.vm.java
 */
package com.app.mpadmin.dao;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.apache.log4j.Logger;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.app.mpadmin.domain.TdTagRelation;
import com.app.mpadmin.repository.TdTagRelationGenerator;
import com.app.mpadmin.dao.TdTagRelationDao;

/**
 * Integration test on TdTagRelationDaoImpl
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class TdTagRelationDaoImplIT {
    @SuppressWarnings("unused")
    private static final Logger log = Logger.getLogger(TdTagRelationDaoImplIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private TdTagRelationDao tdTagRelationDao;

    @Inject
    private TdTagRelationGenerator tdTagRelationGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        TdTagRelation tdTagRelation = tdTagRelationGenerator.getTdTagRelation();

        // add it to a Set before saving (force equals/hashcode)
        Set<TdTagRelation> set = newHashSet(tdTagRelation, tdTagRelation);
        assertThat(set).hasSize(1);

        tdTagRelationDao.save(tdTagRelation);
        entityManager.flush();

        // reload it from cache and check equality
        TdTagRelation model = new TdTagRelation();
        model.setTagRelationId(tdTagRelation.getTagRelationId());
        assertThat(tdTagRelation).isEqualTo(tdTagRelationDao.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // since you use a business key, equality must be preserved.
        assertThat(tdTagRelation).isEqualTo(tdTagRelationDao.get(model));
    }
}