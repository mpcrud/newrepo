/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/dao/DaoImplIT.e.vm.java
 */
package com.app.mpadmin.dao;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.apache.log4j.Logger;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.app.mpadmin.domain.TdOrder;
import com.app.mpadmin.repository.TdOrderGenerator;
import com.app.mpadmin.dao.TdOrderDao;

/**
 * Integration test on TdOrderDaoImpl
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class TdOrderDaoImplIT {
    @SuppressWarnings("unused")
    private static final Logger log = Logger.getLogger(TdOrderDaoImplIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private TdOrderDao tdOrderDao;

    @Inject
    private TdOrderGenerator tdOrderGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        TdOrder tdOrder = tdOrderGenerator.getTdOrder();

        // add it to a Set before saving (force equals/hashcode)
        Set<TdOrder> set = newHashSet(tdOrder, tdOrder);
        assertThat(set).hasSize(1);

        tdOrderDao.save(tdOrder);
        entityManager.flush();

        // reload it from cache and check equality
        TdOrder model = new TdOrder();
        model.setOrderId(tdOrder.getOrderId());
        assertThat(tdOrder).isEqualTo(tdOrderDao.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(tdOrder.getId()).isEqualTo(tdOrderDao.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(tdOrder).isNotEqualTo(tdOrderDao.get(model));
    }
}