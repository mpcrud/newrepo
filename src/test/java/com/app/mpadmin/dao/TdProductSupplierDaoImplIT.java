/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/dao/DaoImplIT.e.vm.java
 */
package com.app.mpadmin.dao;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.apache.log4j.Logger;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.app.mpadmin.domain.TdProductSupplier;
import com.app.mpadmin.repository.TdProductSupplierGenerator;
import com.app.mpadmin.dao.TdProductSupplierDao;

/**
 * Integration test on TdProductSupplierDaoImpl
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class TdProductSupplierDaoImplIT {
    @SuppressWarnings("unused")
    private static final Logger log = Logger.getLogger(TdProductSupplierDaoImplIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private TdProductSupplierDao tdProductSupplierDao;

    @Inject
    private TdProductSupplierGenerator tdProductSupplierGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        TdProductSupplier tdProductSupplier = tdProductSupplierGenerator.getTdProductSupplier();

        // add it to a Set before saving (force equals/hashcode)
        Set<TdProductSupplier> set = newHashSet(tdProductSupplier, tdProductSupplier);
        assertThat(set).hasSize(1);

        tdProductSupplierDao.save(tdProductSupplier);
        entityManager.flush();

        // reload it from cache and check equality
        TdProductSupplier model = new TdProductSupplier();
        model.setProductSupplierId(tdProductSupplier.getProductSupplierId());
        assertThat(tdProductSupplier).isEqualTo(tdProductSupplierDao.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(tdProductSupplier.getId()).isEqualTo(tdProductSupplierDao.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(tdProductSupplier).isNotEqualTo(tdProductSupplierDao.get(model));
    }
}