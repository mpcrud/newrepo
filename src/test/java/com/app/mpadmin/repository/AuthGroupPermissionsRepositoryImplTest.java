/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.AuthGroupPermissions;
import com.app.mpadmin.repository.AuthGroupPermissionsRepositoryImpl;
import com.app.mpadmin.dao.AuthGroupPermissionsDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on AuthGroupPermissionsRepositoryImpl
 */
public class AuthGroupPermissionsRepositoryImplTest {

    private AuthGroupPermissionsRepositoryImpl authGroupPermissionsRepositoryImpl;
    private AuthGroupPermissionsDao authGroupPermissionsDao;

    @Before
    public void setUp() {
        // called before each test.
        authGroupPermissionsRepositoryImpl = new AuthGroupPermissionsRepositoryImpl();
        authGroupPermissionsDao = mock(AuthGroupPermissionsDao.class);
        authGroupPermissionsRepositoryImpl.setAuthGroupPermissionsDao(authGroupPermissionsDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        AuthGroupPermissions none = null;

        when(authGroupPermissionsDao.findUniqueOrNone(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenReturn(none);

        AuthGroupPermissions result = authGroupPermissionsRepositoryImpl.findUniqueOrNone(new AuthGroupPermissions());

        assertThat(result).isNull();
        verify(authGroupPermissionsDao, times(1)).findUniqueOrNone(any(AuthGroupPermissions.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        AuthGroupPermissions unique = new AuthGroupPermissions();

        when(authGroupPermissionsDao.findUniqueOrNone(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenReturn(unique);

        AuthGroupPermissions result = authGroupPermissionsRepositoryImpl.findUniqueOrNone(new AuthGroupPermissions());

        assertThat(result).isNotNull();
        verify(authGroupPermissionsDao, times(1)).findUniqueOrNone(any(AuthGroupPermissions.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(authGroupPermissionsDao.findUniqueOrNone(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authGroupPermissionsRepositoryImpl.findUniqueOrNone(new AuthGroupPermissions());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(authGroupPermissionsDao.findUnique(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        authGroupPermissionsRepositoryImpl.findUnique(new AuthGroupPermissions());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        AuthGroupPermissions unique = new AuthGroupPermissions();

        when(authGroupPermissionsDao.findUnique(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenReturn(unique);

        AuthGroupPermissions result = authGroupPermissionsRepositoryImpl.findUnique(new AuthGroupPermissions());

        assertThat(result).isNotNull();
        verify(authGroupPermissionsDao, times(1)).findUnique(any(AuthGroupPermissions.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(authGroupPermissionsDao.findUnique(any(AuthGroupPermissions.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authGroupPermissionsRepositoryImpl.findUnique(new AuthGroupPermissions());
    }
}