/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.TdState;
import com.app.mpadmin.repository.TdStateRepositoryImpl;
import com.app.mpadmin.dao.TdStateDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on TdStateRepositoryImpl
 */
public class TdStateRepositoryImplTest {

    private TdStateRepositoryImpl tdStateRepositoryImpl;
    private TdStateDao tdStateDao;

    @Before
    public void setUp() {
        // called before each test.
        tdStateRepositoryImpl = new TdStateRepositoryImpl();
        tdStateDao = mock(TdStateDao.class);
        tdStateRepositoryImpl.setTdStateDao(tdStateDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        TdState none = null;

        when(tdStateDao.findUniqueOrNone(any(TdState.class), any(SearchParameters.class))).thenReturn(none);

        TdState result = tdStateRepositoryImpl.findUniqueOrNone(new TdState());

        assertThat(result).isNull();
        verify(tdStateDao, times(1)).findUniqueOrNone(any(TdState.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        TdState unique = new TdState();

        when(tdStateDao.findUniqueOrNone(any(TdState.class), any(SearchParameters.class))).thenReturn(unique);

        TdState result = tdStateRepositoryImpl.findUniqueOrNone(new TdState());

        assertThat(result).isNotNull();
        verify(tdStateDao, times(1)).findUniqueOrNone(any(TdState.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(tdStateDao.findUniqueOrNone(any(TdState.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdStateRepositoryImpl.findUniqueOrNone(new TdState());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(tdStateDao.findUnique(any(TdState.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        tdStateRepositoryImpl.findUnique(new TdState());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        TdState unique = new TdState();

        when(tdStateDao.findUnique(any(TdState.class), any(SearchParameters.class))).thenReturn(unique);

        TdState result = tdStateRepositoryImpl.findUnique(new TdState());

        assertThat(result).isNotNull();
        verify(tdStateDao, times(1)).findUnique(any(TdState.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(tdStateDao.findUnique(any(TdState.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdStateRepositoryImpl.findUnique(new TdState());
    }
}