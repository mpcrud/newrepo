/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.TdOrder;
import com.app.mpadmin.repository.TdOrderRepositoryImpl;
import com.app.mpadmin.dao.TdOrderDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on TdOrderRepositoryImpl
 */
public class TdOrderRepositoryImplTest {

    private TdOrderRepositoryImpl tdOrderRepositoryImpl;
    private TdOrderDao tdOrderDao;

    @Before
    public void setUp() {
        // called before each test.
        tdOrderRepositoryImpl = new TdOrderRepositoryImpl();
        tdOrderDao = mock(TdOrderDao.class);
        tdOrderRepositoryImpl.setTdOrderDao(tdOrderDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        TdOrder none = null;

        when(tdOrderDao.findUniqueOrNone(any(TdOrder.class), any(SearchParameters.class))).thenReturn(none);

        TdOrder result = tdOrderRepositoryImpl.findUniqueOrNone(new TdOrder());

        assertThat(result).isNull();
        verify(tdOrderDao, times(1)).findUniqueOrNone(any(TdOrder.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        TdOrder unique = new TdOrder();

        when(tdOrderDao.findUniqueOrNone(any(TdOrder.class), any(SearchParameters.class))).thenReturn(unique);

        TdOrder result = tdOrderRepositoryImpl.findUniqueOrNone(new TdOrder());

        assertThat(result).isNotNull();
        verify(tdOrderDao, times(1)).findUniqueOrNone(any(TdOrder.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(tdOrderDao.findUniqueOrNone(any(TdOrder.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdOrderRepositoryImpl.findUniqueOrNone(new TdOrder());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(tdOrderDao.findUnique(any(TdOrder.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        tdOrderRepositoryImpl.findUnique(new TdOrder());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        TdOrder unique = new TdOrder();

        when(tdOrderDao.findUnique(any(TdOrder.class), any(SearchParameters.class))).thenReturn(unique);

        TdOrder result = tdOrderRepositoryImpl.findUnique(new TdOrder());

        assertThat(result).isNotNull();
        verify(tdOrderDao, times(1)).findUnique(any(TdOrder.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(tdOrderDao.findUnique(any(TdOrder.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdOrderRepositoryImpl.findUnique(new TdOrder());
    }
}