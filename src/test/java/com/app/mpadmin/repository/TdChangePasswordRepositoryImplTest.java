/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.TdChangePassword;
import com.app.mpadmin.repository.TdChangePasswordRepositoryImpl;
import com.app.mpadmin.dao.TdChangePasswordDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on TdChangePasswordRepositoryImpl
 */
public class TdChangePasswordRepositoryImplTest {

    private TdChangePasswordRepositoryImpl tdChangePasswordRepositoryImpl;
    private TdChangePasswordDao tdChangePasswordDao;

    @Before
    public void setUp() {
        // called before each test.
        tdChangePasswordRepositoryImpl = new TdChangePasswordRepositoryImpl();
        tdChangePasswordDao = mock(TdChangePasswordDao.class);
        tdChangePasswordRepositoryImpl.setTdChangePasswordDao(tdChangePasswordDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        TdChangePassword none = null;

        when(tdChangePasswordDao.findUniqueOrNone(any(TdChangePassword.class), any(SearchParameters.class))).thenReturn(none);

        TdChangePassword result = tdChangePasswordRepositoryImpl.findUniqueOrNone(new TdChangePassword());

        assertThat(result).isNull();
        verify(tdChangePasswordDao, times(1)).findUniqueOrNone(any(TdChangePassword.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        TdChangePassword unique = new TdChangePassword();

        when(tdChangePasswordDao.findUniqueOrNone(any(TdChangePassword.class), any(SearchParameters.class))).thenReturn(unique);

        TdChangePassword result = tdChangePasswordRepositoryImpl.findUniqueOrNone(new TdChangePassword());

        assertThat(result).isNotNull();
        verify(tdChangePasswordDao, times(1)).findUniqueOrNone(any(TdChangePassword.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(tdChangePasswordDao.findUniqueOrNone(any(TdChangePassword.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdChangePasswordRepositoryImpl.findUniqueOrNone(new TdChangePassword());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(tdChangePasswordDao.findUnique(any(TdChangePassword.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        tdChangePasswordRepositoryImpl.findUnique(new TdChangePassword());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        TdChangePassword unique = new TdChangePassword();

        when(tdChangePasswordDao.findUnique(any(TdChangePassword.class), any(SearchParameters.class))).thenReturn(unique);

        TdChangePassword result = tdChangePasswordRepositoryImpl.findUnique(new TdChangePassword());

        assertThat(result).isNotNull();
        verify(tdChangePasswordDao, times(1)).findUnique(any(TdChangePassword.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(tdChangePasswordDao.findUnique(any(TdChangePassword.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdChangePasswordRepositoryImpl.findUnique(new TdChangePassword());
    }
}