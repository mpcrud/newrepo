/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.AuthUserGroups;
import com.app.mpadmin.repository.AuthUserGroupsRepositoryImpl;
import com.app.mpadmin.dao.AuthUserGroupsDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on AuthUserGroupsRepositoryImpl
 */
public class AuthUserGroupsRepositoryImplTest {

    private AuthUserGroupsRepositoryImpl authUserGroupsRepositoryImpl;
    private AuthUserGroupsDao authUserGroupsDao;

    @Before
    public void setUp() {
        // called before each test.
        authUserGroupsRepositoryImpl = new AuthUserGroupsRepositoryImpl();
        authUserGroupsDao = mock(AuthUserGroupsDao.class);
        authUserGroupsRepositoryImpl.setAuthUserGroupsDao(authUserGroupsDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        AuthUserGroups none = null;

        when(authUserGroupsDao.findUniqueOrNone(any(AuthUserGroups.class), any(SearchParameters.class))).thenReturn(none);

        AuthUserGroups result = authUserGroupsRepositoryImpl.findUniqueOrNone(new AuthUserGroups());

        assertThat(result).isNull();
        verify(authUserGroupsDao, times(1)).findUniqueOrNone(any(AuthUserGroups.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        AuthUserGroups unique = new AuthUserGroups();

        when(authUserGroupsDao.findUniqueOrNone(any(AuthUserGroups.class), any(SearchParameters.class))).thenReturn(unique);

        AuthUserGroups result = authUserGroupsRepositoryImpl.findUniqueOrNone(new AuthUserGroups());

        assertThat(result).isNotNull();
        verify(authUserGroupsDao, times(1)).findUniqueOrNone(any(AuthUserGroups.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(authUserGroupsDao.findUniqueOrNone(any(AuthUserGroups.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authUserGroupsRepositoryImpl.findUniqueOrNone(new AuthUserGroups());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(authUserGroupsDao.findUnique(any(AuthUserGroups.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        authUserGroupsRepositoryImpl.findUnique(new AuthUserGroups());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        AuthUserGroups unique = new AuthUserGroups();

        when(authUserGroupsDao.findUnique(any(AuthUserGroups.class), any(SearchParameters.class))).thenReturn(unique);

        AuthUserGroups result = authUserGroupsRepositoryImpl.findUnique(new AuthUserGroups());

        assertThat(result).isNotNull();
        verify(authUserGroupsDao, times(1)).findUnique(any(AuthUserGroups.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(authUserGroupsDao.findUnique(any(AuthUserGroups.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authUserGroupsRepositoryImpl.findUnique(new AuthUserGroups());
    }
}