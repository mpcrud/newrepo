/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.TdPermission;
import com.app.mpadmin.repository.TdPermissionRepositoryImpl;
import com.app.mpadmin.dao.TdPermissionDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on TdPermissionRepositoryImpl
 */
public class TdPermissionRepositoryImplTest {

    private TdPermissionRepositoryImpl tdPermissionRepositoryImpl;
    private TdPermissionDao tdPermissionDao;

    @Before
    public void setUp() {
        // called before each test.
        tdPermissionRepositoryImpl = new TdPermissionRepositoryImpl();
        tdPermissionDao = mock(TdPermissionDao.class);
        tdPermissionRepositoryImpl.setTdPermissionDao(tdPermissionDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        TdPermission none = null;

        when(tdPermissionDao.findUniqueOrNone(any(TdPermission.class), any(SearchParameters.class))).thenReturn(none);

        TdPermission result = tdPermissionRepositoryImpl.findUniqueOrNone(new TdPermission());

        assertThat(result).isNull();
        verify(tdPermissionDao, times(1)).findUniqueOrNone(any(TdPermission.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        TdPermission unique = new TdPermission();

        when(tdPermissionDao.findUniqueOrNone(any(TdPermission.class), any(SearchParameters.class))).thenReturn(unique);

        TdPermission result = tdPermissionRepositoryImpl.findUniqueOrNone(new TdPermission());

        assertThat(result).isNotNull();
        verify(tdPermissionDao, times(1)).findUniqueOrNone(any(TdPermission.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(tdPermissionDao.findUniqueOrNone(any(TdPermission.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdPermissionRepositoryImpl.findUniqueOrNone(new TdPermission());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(tdPermissionDao.findUnique(any(TdPermission.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        tdPermissionRepositoryImpl.findUnique(new TdPermission());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        TdPermission unique = new TdPermission();

        when(tdPermissionDao.findUnique(any(TdPermission.class), any(SearchParameters.class))).thenReturn(unique);

        TdPermission result = tdPermissionRepositoryImpl.findUnique(new TdPermission());

        assertThat(result).isNotNull();
        verify(tdPermissionDao, times(1)).findUnique(any(TdPermission.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(tdPermissionDao.findUnique(any(TdPermission.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        tdPermissionRepositoryImpl.findUnique(new TdPermission());
    }
}