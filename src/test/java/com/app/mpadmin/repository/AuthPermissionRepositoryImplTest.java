/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.AuthPermission;
import com.app.mpadmin.repository.AuthPermissionRepositoryImpl;
import com.app.mpadmin.dao.AuthPermissionDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on AuthPermissionRepositoryImpl
 */
public class AuthPermissionRepositoryImplTest {

    private AuthPermissionRepositoryImpl authPermissionRepositoryImpl;
    private AuthPermissionDao authPermissionDao;

    @Before
    public void setUp() {
        // called before each test.
        authPermissionRepositoryImpl = new AuthPermissionRepositoryImpl();
        authPermissionDao = mock(AuthPermissionDao.class);
        authPermissionRepositoryImpl.setAuthPermissionDao(authPermissionDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        AuthPermission none = null;

        when(authPermissionDao.findUniqueOrNone(any(AuthPermission.class), any(SearchParameters.class))).thenReturn(none);

        AuthPermission result = authPermissionRepositoryImpl.findUniqueOrNone(new AuthPermission());

        assertThat(result).isNull();
        verify(authPermissionDao, times(1)).findUniqueOrNone(any(AuthPermission.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        AuthPermission unique = new AuthPermission();

        when(authPermissionDao.findUniqueOrNone(any(AuthPermission.class), any(SearchParameters.class))).thenReturn(unique);

        AuthPermission result = authPermissionRepositoryImpl.findUniqueOrNone(new AuthPermission());

        assertThat(result).isNotNull();
        verify(authPermissionDao, times(1)).findUniqueOrNone(any(AuthPermission.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(authPermissionDao.findUniqueOrNone(any(AuthPermission.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authPermissionRepositoryImpl.findUniqueOrNone(new AuthPermission());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(authPermissionDao.findUnique(any(AuthPermission.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        authPermissionRepositoryImpl.findUnique(new AuthPermission());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        AuthPermission unique = new AuthPermission();

        when(authPermissionDao.findUnique(any(AuthPermission.class), any(SearchParameters.class))).thenReturn(unique);

        AuthPermission result = authPermissionRepositoryImpl.findUnique(new AuthPermission());

        assertThat(result).isNotNull();
        verify(authPermissionDao, times(1)).findUnique(any(AuthPermission.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(authPermissionDao.findUnique(any(AuthPermission.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authPermissionRepositoryImpl.findUnique(new AuthPermission());
    }
}