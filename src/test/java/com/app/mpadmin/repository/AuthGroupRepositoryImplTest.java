/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/ServiceImplTest.e.vm.java
 */
package com.app.mpadmin.repository;

import static org.fest.assertions.Assertions.*;
import static org.mockito.Matchers.*;
import static org.mockito.Mockito.*;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Before;
import org.junit.Test;

import com.app.mpadmin.domain.AuthGroup;
import com.app.mpadmin.repository.AuthGroupRepositoryImpl;
import com.app.mpadmin.dao.AuthGroupDao;
import com.app.mpadmin.dao.support.SearchParameters;

/**
 * Unit test on AuthGroupRepositoryImpl
 */
public class AuthGroupRepositoryImplTest {

    private AuthGroupRepositoryImpl authGroupRepositoryImpl;
    private AuthGroupDao authGroupDao;

    @Before
    public void setUp() {
        // called before each test.
        authGroupRepositoryImpl = new AuthGroupRepositoryImpl();
        authGroupDao = mock(AuthGroupDao.class);
        authGroupRepositoryImpl.setAuthGroupDao(authGroupDao);
    }

    @Test
    public void testFindUniqueOrNoneCaseNone() {
        AuthGroup none = null;

        when(authGroupDao.findUniqueOrNone(any(AuthGroup.class), any(SearchParameters.class))).thenReturn(none);

        AuthGroup result = authGroupRepositoryImpl.findUniqueOrNone(new AuthGroup());

        assertThat(result).isNull();
        verify(authGroupDao, times(1)).findUniqueOrNone(any(AuthGroup.class), any(SearchParameters.class));
    }

    @Test
    public void testFindUniqueOrNoneCaseUnique() {
        AuthGroup unique = new AuthGroup();

        when(authGroupDao.findUniqueOrNone(any(AuthGroup.class), any(SearchParameters.class))).thenReturn(unique);

        AuthGroup result = authGroupRepositoryImpl.findUniqueOrNone(new AuthGroup());

        assertThat(result).isNotNull();
        verify(authGroupDao, times(1)).findUniqueOrNone(any(AuthGroup.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueOrNoneCaseMultiple() {
        when(authGroupDao.findUniqueOrNone(any(AuthGroup.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authGroupRepositoryImpl.findUniqueOrNone(new AuthGroup());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void testFindUniqueCaseNone() {
        when(authGroupDao.findUnique(any(AuthGroup.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        authGroupRepositoryImpl.findUnique(new AuthGroup());
    }

    @Test
    public void testFindUniqueCaseUnique() {
        AuthGroup unique = new AuthGroup();

        when(authGroupDao.findUnique(any(AuthGroup.class), any(SearchParameters.class))).thenReturn(unique);

        AuthGroup result = authGroupRepositoryImpl.findUnique(new AuthGroup());

        assertThat(result).isNotNull();
        verify(authGroupDao, times(1)).findUnique(any(AuthGroup.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void testFindUniqueCaseMultiple() {
        when(authGroupDao.findUnique(any(AuthGroup.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        authGroupRepositoryImpl.findUnique(new AuthGroup());
    }
}