/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/domain/ModelTest.e.vm.java
 */
package com.app.mpadmin.domain;

import java.io.*;
import java.util.*;

import static org.junit.Assert.*;
import org.junit.Test;

import com.app.mpadmin.util.*;
import static javax.persistence.CascadeType.PERSIST;
import static javax.persistence.FetchType.LAZY;
import static javax.persistence.TemporalType.TIMESTAMP;
import static org.hibernate.annotations.CacheConcurrencyStrategy.NONSTRICT_READ_WRITE;
import java.io.Serializable;
import java.util.Date;
import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.JoinColumn;
import javax.persistence.ManyToOne;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.Transient;
import org.apache.log4j.Logger;
import org.hibernate.annotations.Cache;
import com.app.mpadmin.domain.TdTopic;
import com.app.mpadmin.domain.TdUserAuth;

/**
 * Basic tests for TdVote
 */
@SuppressWarnings("unused")
public class TdVoteTest {

    // test unique primary key
    @Test
    public void newInstanceHasNoPrimaryKey() {
        TdVote model = new TdVote();
        assertFalse(model.isIdSet());
    }

    @Test
    public void isIdSetReturnsTrue() {
        TdVote model = new TdVote();
        model.setVoteId(ValueGenerator.getUniqueInteger());
        assertNotNull(model.getVoteId());
        assertTrue(model.isIdSet());
    }

    // test columns methods

    //-------------------------------------------------------------
    // Many to One:  TdVote.userId ==> TdUserAuth.userId
    //-------------------------------------------------------------

    @Test
    public void manyToOne_setUser() {
        TdVote many = new TdVote();

        // init
        TdUserAuth one = new TdUserAuth();

        one.setUserId(ValueGenerator.getUniqueInteger());
        many.setUser(one);

        // make sure it is propagated properly
        assertNotNull(many.getUserId());
        assertEquals(one, many.getUser());
        assertSame(many.getUserId(), one.getUserId());
        // now set it to back to null
        many.setUser(null);

        // make sure null is propagated properly
        assertNull(many.getUser());

        // make sure it is propagated on fk column as well
        assertNull(many.getUserId());
    }

    @Test
    public void manyToOne_setTopic() {
        TdVote many = new TdVote();

        // init
        TdTopic one = new TdTopic();

        one.setTopicId(ValueGenerator.getUniqueInteger());
        many.setTopic(one);

        // make sure it is propagated properly
        assertNotNull(many.getTopicId());
        assertEquals(one, many.getTopic());
        assertSame(many.getTopicId(), one.getTopicId());
        // now set it to back to null
        many.setTopic(null);

        // make sure null is propagated properly
        assertNull(many.getTopic());

        // make sure it is propagated on fk column as well
        assertNull(many.getTopicId());
    }

    @Test
    public void toStringNotNull() {
        TdVote model = new TdVote();
        assertNotNull(model.toString());
    }

    @Test
    public void equalsUsingPk() {
        TdVote model1 = new TdVote();
        TdVote model2 = new TdVote();

        Integer voteId = ValueGenerator.getUniqueInteger();
        model1.setVoteId(voteId);
        model2.setVoteId(voteId);

        model1.setVoteType(1);
        model2.setVoteType(1);

        model1.setCreationDate(new Date());
        model2.setCreationDate(new Date());

        model1.setUpdationDate(new Date());
        model2.setUpdationDate(new Date());

        model1.setIsActive(1);
        model2.setIsActive(1);

        model1.setVoteStatus(1);
        model2.setVoteStatus(1);

        model1.setCommentId(1);
        model2.setCommentId(1);
        assertTrue(model1.isIdSet());
        assertTrue(model2.isIdSet());
        assertTrue(model1.hashCode() == model2.hashCode());
        assertTrue(model1.equals(model2));
        assertTrue(model2.equals(model1));
    }
}