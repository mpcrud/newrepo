/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-jsf2-primefaces:src/main/java/domain/LazyDataModel.e.vm.java
 */
package com.app.mpadmin.web.domain;

import java.util.List;
import java.util.Map;
import javax.inject.Inject;

import org.primefaces.model.SortOrder;

import com.app.mpadmin.dao.support.SearchParameters;
import com.app.mpadmin.domain.TdMakeSubmodel;
import com.app.mpadmin.repository.TdMakeSubmodelRepository;
import com.app.mpadmin.web.domain.support.GenericLazyDataModel;

/**
 * Provides server-side pagination for search.
 *
 * @see http://jira.springframework.org/browse/SWF-1224 to avoid instanciate it as a var from your flow 
 */
public class TdMakeSubmodelLazyDataModel extends GenericLazyDataModel<TdMakeSubmodel> {
    private static final long serialVersionUID = 1L;

    @Inject
    transient private TdMakeSubmodelRepository tdMakeSubmodelRepository;
    @Inject
    transient private TdMakeSubmodelSearchForm tdMakeSubmodelSearchForm;

List<TdMakeSubmodel> datasource;

    @Override
    public TdMakeSubmodel getRowData(String rowKey) {
        for(TdMakeSubmodel tdMakeSubmodel : datasource) {
            if(tdMakeSubmodel.getMakeSubmodelId().equals(rowKey))
                return tdMakeSubmodel;
        }

        return null;
    }

    @Override
    public Object getRowKey(TdMakeSubmodel tdMakeSubmodel)
    {
         return tdMakeSubmodel.getMakeSubmodelId();
    }


    /**
     * Prepare the search parameters and call the tdMakeSubmodelRepository finder.
     * Automatically called by PrimeFaces component.
     */
    @Override
    public List<TdMakeSubmodel> load(int first, int pageSize, String sortField, SortOrder sortOrder, Map<String, String> filters) {
        SearchParameters sp = tdMakeSubmodelSearchForm.getSearchParameters();

        // ranges
        sp.clearRanges();
        sp.addRange(tdMakeSubmodelSearchForm.getMakeModelIdRange());
        sp.addRange(tdMakeSubmodelSearchForm.getSubmodelLaunchYearRange());
        sp.addRange(tdMakeSubmodelSearchForm.getFuelTypeRange());
        sp.addRange(tdMakeSubmodelSearchForm.getTransmissionTypeRange());
        sp.addRange(tdMakeSubmodelSearchForm.getWheeldriveTypeRange());
        sp.addRange(tdMakeSubmodelSearchForm.getEngineCapacityRange());
        sp.addRange(tdMakeSubmodelSearchForm.getBodyTypeRange());
        sp.addRange(tdMakeSubmodelSearchForm.getDoorCountRange());
        sp.addRange(tdMakeSubmodelSearchForm.getGearCountRange());
        sp.addRange(tdMakeSubmodelSearchForm.getFuelDeliveryRange());
        sp.addRange(tdMakeSubmodelSearchForm.getValvesCountRange());
        sp.addRange(tdMakeSubmodelSearchForm.getIsActiveRange());
        sp.addRange(tdMakeSubmodelSearchForm.getBhpRange());
        sp.addRange(tdMakeSubmodelSearchForm.getTorqueRange());
        sp.addRange(tdMakeSubmodelSearchForm.getSubmodelTypeRange());
        sp.addRange(tdMakeSubmodelSearchForm.getSpecialEditionYearRange());
        sp.addRange(tdMakeSubmodelSearchForm.getMakesubmodelStatusRange());
        sp.addRange(tdMakeSubmodelSearchForm.getUpdationDateRange());

        TdMakeSubmodel tdMakeSubmodel = tdMakeSubmodelSearchForm.getTdMakeSubmodel();
        setRowCount(tdMakeSubmodelRepository.findCount(tdMakeSubmodel, sp)); // total count so the paginator may display the total number of pages
        populateSearchParameters(sp, first, pageSize, sortField, sortOrder, filters); // load one page of data
        datasource = tdMakeSubmodelRepository.find(tdMakeSubmodel, sp);
        return datasource;
    }
}