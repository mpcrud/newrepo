/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-jsf2-primefaces:src/main/java/domain/LazyDataModel.e.vm.java
 */
package com.app.mpadmin.web.domain;

import java.util.List;
import java.util.Map;
import javax.inject.Inject;

import org.primefaces.model.SortOrder;

import com.app.mpadmin.dao.support.SearchParameters;
import com.app.mpadmin.domain.TdShipmentVendor;
import com.app.mpadmin.repository.TdShipmentVendorRepository;
import com.app.mpadmin.web.domain.support.GenericLazyDataModel;

/**
 * Provides server-side pagination for search.
 *
 * @see http://jira.springframework.org/browse/SWF-1224 to avoid instanciate it as a var from your flow 
 */
public class TdShipmentVendorLazyDataModel extends GenericLazyDataModel<TdShipmentVendor> {
    private static final long serialVersionUID = 1L;

    @Inject
    transient private TdShipmentVendorRepository tdShipmentVendorRepository;
    @Inject
    transient private TdShipmentVendorSearchForm tdShipmentVendorSearchForm;

List<TdShipmentVendor> datasource;

    @Override
    public TdShipmentVendor getRowData(String rowKey) {
        for(TdShipmentVendor tdShipmentVendor : datasource) {
            if(tdShipmentVendor.getShipmentVendorId().equals(rowKey))
                return tdShipmentVendor;
        }

        return null;
    }

    @Override
    public Object getRowKey(TdShipmentVendor tdShipmentVendor)
    {
         return tdShipmentVendor.getShipmentVendorId();
    }


    /**
     * Prepare the search parameters and call the tdShipmentVendorRepository finder.
     * Automatically called by PrimeFaces component.
     */
    @Override
    public List<TdShipmentVendor> load(int first, int pageSize, String sortField, SortOrder sortOrder, Map<String, String> filters) {
        SearchParameters sp = tdShipmentVendorSearchForm.getSearchParameters();

        // ranges
        sp.clearRanges();
        sp.addRange(tdShipmentVendorSearchForm.getShipmentVendorStandardMrpCostRange());
        sp.addRange(tdShipmentVendorSearchForm.getShipmentVendorStandardDiscountRange());
        sp.addRange(tdShipmentVendorSearchForm.getShipmentDurationRange());
        sp.addRange(tdShipmentVendorSearchForm.getShipmentVendorTypeRange());
        sp.addRange(tdShipmentVendorSearchForm.getUpdationDateRange());
        sp.addRange(tdShipmentVendorSearchForm.getShipmentVendorIdRange());
        // property selectors
        sp.clearPropertySelectors();
        sp.addPropertySelector(tdShipmentVendorSearchForm.getShipmentVendorStatusSelector());

        TdShipmentVendor tdShipmentVendor = tdShipmentVendorSearchForm.getTdShipmentVendor();
        setRowCount(tdShipmentVendorRepository.findCount(tdShipmentVendor, sp)); // total count so the paginator may display the total number of pages
        populateSearchParameters(sp, first, pageSize, sortField, sortOrder, filters); // load one page of data
        datasource = tdShipmentVendorRepository.find(tdShipmentVendor, sp);
        return datasource;
    }
}