/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-jsf2-primefaces:src/main/java/domain/LazyDataModel.e.vm.java
 */
package com.app.mpadmin.web.domain;

import java.util.List;
import java.util.Map;
import javax.inject.Inject;

import org.primefaces.model.SortOrder;

import com.app.mpadmin.dao.support.SearchParameters;
import com.app.mpadmin.domain.TdProductSupplier;
import com.app.mpadmin.repository.TdProductSupplierRepository;
import com.app.mpadmin.web.domain.support.GenericLazyDataModel;

/**
 * Provides server-side pagination for search.
 *
 * @see http://jira.springframework.org/browse/SWF-1224 to avoid instanciate it as a var from your flow 
 */
public class TdProductSupplierLazyDataModel extends GenericLazyDataModel<TdProductSupplier> {
    private static final long serialVersionUID = 1L;

    @Inject
    transient private TdProductSupplierRepository tdProductSupplierRepository;
    @Inject
    transient private TdProductSupplierSearchForm tdProductSupplierSearchForm;

    List<TdProductSupplier> datasource;

    @Override
    public TdProductSupplier getRowData(String rowKey) {
        for(TdProductSupplier tdProductSupplier : datasource) {
            if(tdProductSupplier.getProductSupplierId().equals(rowKey))
                return tdProductSupplier;
        }

        return null;
    }

    @Override
    public Object getRowKey(TdProductSupplier tdProductSupplier)
    {
         return tdProductSupplier.getProductSupplierId();
    }


    /**
     * Prepare the search parameters and call the tdProductSupplierRepository finder.
     * Automatically called by PrimeFaces component.
     */
    @Override
    public List<TdProductSupplier> load(int first, int pageSize, String sortField, SortOrder sortOrder, Map<String, String> filters) {
        SearchParameters sp = tdProductSupplierSearchForm.getSearchParameters();

        // ranges
        sp.clearRanges();
        sp.addRange(tdProductSupplierSearchForm.getProductSupplierRatingRange());
        sp.addRange(tdProductSupplierSearchForm.getProductSupplierTypeRange());
        sp.addRange(tdProductSupplierSearchForm.getUpdationDateRange());

        // property selectors
        sp.clearPropertySelectors();
        sp.addPropertySelector(tdProductSupplierSearchForm.getProductSupplierStatusSelector());

        TdProductSupplier tdProductSupplier = tdProductSupplierSearchForm.getTdProductSupplier();
        setRowCount(tdProductSupplierRepository.findCount(tdProductSupplier, sp)); // total count so the paginator may display the total number of pages
        populateSearchParameters(sp, first, pageSize, sortField, sortOrder, filters); // load one page of data
        datasource = tdProductSupplierRepository.find(tdProductSupplier, sp);
        return datasource;
    }
}