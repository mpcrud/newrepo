/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/main/java/project/domain/CompositePk.cpk.vm.java
 */
package com.app.mpadmin.domain;

import static org.apache.commons.lang.StringUtils.isNotEmpty;

import com.google.common.base.Objects;

import java.io.Serializable;
import javax.persistence.Column;
import javax.persistence.Embeddable;
import javax.persistence.Transient;
import javax.validation.constraints.NotNull;

@Embeddable
public class TempTdTagGrpTagMappingPk implements Comparable<TempTdTagGrpTagMappingPk>, Serializable {
    static final private long serialVersionUID = 1L;

    private Integer tagGrpId;
    private Integer tagId;

    public TempTdTagGrpTagMappingPk() {
    }

    public TempTdTagGrpTagMappingPk(Integer tagGrpId, Integer tagId) {
        this.tagGrpId = tagGrpId;
        this.tagId = tagId;
    }

    /**
     * Helper to determine if this composite primary key can be considered as set or not.
     */
    @Transient
    public boolean isTempTdTagGrpTagMappingPkSet() {
        return isTagGrpIdSet() && isTagIdSet();
    }

    /**
     * Helper method to determine if this instance is considered empty, that is,
     * if all the non primary key columns are null.
     */
    @Transient
    public boolean isEmpty() {
        return !isTagGrpIdSet() && !isTagIdSet();
    }

    //-- [tagGrpId] ------------------------------

    @NotNull
    @Column(name = "tag_grp_id", nullable = false, precision = 10)
    public Integer getTagGrpId() {
        return tagGrpId;
    }

    public void setTagGrpId(Integer tagGrpId) {
        this.tagGrpId = tagGrpId;
    }

    /**
     * Helper that determines if this attribute is set or not.
     */
    @Transient
    public boolean isTagGrpIdSet() {
        return getTagGrpId() != null;
    }

    //-- [tagId] ------------------------------

    @NotNull
    @Column(name = "tag_id", nullable = false, precision = 10)
    public Integer getTagId() {
        return tagId;
    }

    public void setTagId(Integer tagId) {
        this.tagId = tagId;
    }

    /**
     * Helper that determines if this attribute is set or not.
     */
    @Transient
    public boolean isTagIdSet() {
        return getTagId() != null;
    }

    @Override
    public boolean equals(Object other) {
        return this == other || (other instanceof TempTdTagGrpTagMappingPk && hashCode() == other.hashCode());
    }

    @Override
    public int hashCode() {
        return Objects.hashCode(getTagGrpId(), getTagId());
    }

    @Override
    public int compareTo(TempTdTagGrpTagMappingPk other) {
        return other == null ? -1 : hashCode() - other.hashCode();
    }

    /**
     * Return the string representation of the composite primary key, it should be reversable by version produced by the {@link #String()} method
     */
    @Override
    public String toString() {
        StringBuilder result = new StringBuilder();
        if (isTagGrpIdSet()) {
            result.append(getTagGrpId());
        }
        result.append(":");
        if (isTagIdSet()) {
            result.append(getTagId());
        }

        return result.toString();
    }

    /**
     * Build an instance from a string version produced by the {@link #toString()} method
     */
    public static TempTdTagGrpTagMappingPk fromString(String string) {
        TempTdTagGrpTagMappingPk result = new TempTdTagGrpTagMappingPk();
        String[] values = string.split(":");
        if (isNotEmpty(values[0])) {
            result.setTagGrpId(new Integer(values[0]));
        }
        if (isNotEmpty(values[1])) {
            result.setTagId(new Integer(values[1]));
        }

        return result;
    }
}
