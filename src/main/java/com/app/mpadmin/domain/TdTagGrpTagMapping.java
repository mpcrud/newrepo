/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/main/java/project/domain/Entity.e.vm.java
 */
package com.app.mpadmin.domain;

import static javax.persistence.CascadeType.PERSIST;
import static javax.persistence.FetchType.LAZY;
import static org.hibernate.annotations.CacheConcurrencyStrategy.NONSTRICT_READ_WRITE;
import java.io.Serializable;
import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.JoinColumn;
import javax.persistence.ManyToOne;
import javax.persistence.Table;
import javax.persistence.Transient;
import javax.validation.constraints.NotNull;
import javax.xml.bind.annotation.XmlTransient;
import org.apache.log4j.Logger;
import org.hibernate.annotations.Cache;
import com.app.mpadmin.domain.PersistableHashBuilder;
import com.google.common.base.Objects;

@Entity
@Table(name = "td_tag_grp_tag_mapping")
@Cache(usage = NONSTRICT_READ_WRITE)
public class TdTagGrpTagMapping implements Identifiable<Integer>, Serializable {
    private static final long serialVersionUID = 1L;
    private static final Logger log = Logger.getLogger(TdTagGrpTagMapping.class);

    // Raw attributes
    private Integer grpTagMappingId; // pk
    private Integer tagGrpId;
    private Integer tagId;
    
    //Many to One 
    private TdTagGrp tagGrp;
    private TdTag tag;

    // ---------------------------
    // Constructors
    // ---------------------------

    public TdTagGrpTagMapping() {
    }

    public TdTagGrpTagMapping(Integer primaryKey) {
        setId(primaryKey);
    }

    // ---------------------------
    // Identifiable implementation
    // ---------------------------

    @Override
    @Transient
    @XmlTransient
    public Integer getId() {
        return getGrpTagMappingId();
    }

    @Override
    public void setId(Integer grpTagMappingId) {
        setGrpTagMappingId(grpTagMappingId);
    }

    @Override
    @Transient
    @XmlTransient
    public boolean isIdSet() {
        return isGrpTagMappingIdSet();
    }

    // -------------------------------
    // Getter & Setter
    // -------------------------------

    // -- [grpTagMappingId] ------------------------

    @Column(name = "grp_tag_mapping_id", precision = 10)
    @GeneratedValue
    @Id
    public Integer getGrpTagMappingId() {
        return grpTagMappingId;
    }

    public void setGrpTagMappingId(Integer grpTagMappingId) {
        this.grpTagMappingId = grpTagMappingId;
    }

    @Transient
    public boolean isGrpTagMappingIdSet() {
        return grpTagMappingId != null;
    }

    // -- [tagGrpId] ------------------------
    @NotNull
    @Column(name = "tag_grp_id", precision = 10,insertable=false,updatable=false,nullable=false)
    public Integer getTagGrpId() {
        return tagGrpId;
    }

    public void setTagGrpId(Integer tagGrpId) {
        this.tagGrpId = tagGrpId;
    }

    // -- [tagId] ------------------------

    @NotNull
    @Column(name = "tag_id", precision = 10,insertable=false,updatable=false,nullable=false)
    public Integer getTagId() {
        return tagId;
    }

    public void setTagId(Integer tagId) {
        this.tagId = tagId;
    }
    
    
 // Many to One support
    // --------------------------------------------------------------------

    // - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    // many-to-one: TdTagGrpTagMapping.tagGrpId ==> TdTagGrp.tagGrpId
    // - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

    
    @Cache(usage = NONSTRICT_READ_WRITE)
    @JoinColumn(name = "tag_grp_id")
    @ManyToOne(cascade = PERSIST, fetch = LAZY)
    public TdTagGrp getTagGrp() {
        return tagGrp;
    }

    /**
     * Set the tagGrp without adding this TdTagGrpTagMapping instance on the passed tagGrp
     * If you want to preserve referential integrity we recommend to use
     * instead the corresponding adder method provided by {@link TdTagGrp}
     */
    public void setTagGrp(TdTagGrp tagGrp) {
        this.tagGrp = tagGrp;

        // We set the foreign key property so it can be used by our JPA search by Example facility.
        if (tagGrp != null) {
            setTagGrpId(tagGrp.getTagGrpId());
        } else {
            setTagGrpId(null);
        }
    }

    // - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    // many-to-one: TdTagGrpTagMapping.tagId ==> TdTag.tagId
    // - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

    @Cache(usage = NONSTRICT_READ_WRITE)
    @JoinColumn(name = "tag_id")
    @ManyToOne(cascade = PERSIST, fetch = LAZY)
    public TdTag getTag() {
        return tag;
    }

    /**
     * Set the tag without adding this TdTagGrpTagMapping instance on the passed tag
     * If you want to preserve referential integrity we recommend to use
     * instead the corresponding adder method provided by {@link TdTag}
     */
    public void setTag(TdTag tag) {
        this.tag = tag;

        // We set the foreign key property so it can be used by our JPA search by Example facility.
        if (tag != null) {
            setTagId(tag.getTagId());
        } else {
            setTagId(null);
        }
    }


    /**
     * Set the default values.
     */
    public void initDefaultValues() {
    }

    /**
     * equals implementation using a business key.
     */
    @Override
    public boolean equals(Object other) {
        return this == other || (other instanceof TdTagGrpTagMapping && hashCode() == other.hashCode());
    }

    private PersistableHashBuilder persistableHashBuilder = new PersistableHashBuilder();

    @Override
    public int hashCode() {
        return persistableHashBuilder.hash(log, this);
    }

    /**
     * Construct a readable string representation for this TdTagGrpTagMapping instance.
     * @see java.lang.Object#toString()
     */
    @Override
    public String toString() {
        return Objects.toStringHelper(this) //
                .add("grpTagMappingId", getGrpTagMappingId()) //
                .add("tagGrpId", getTagGrpId()) //
                .add("tagId", getTagId()) //
                .toString();
    }
}