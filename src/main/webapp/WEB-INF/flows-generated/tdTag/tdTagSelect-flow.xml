<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 ! Source code generated by Celerio, a Jaxio product
 ! Want to use Celerio within your company? email us at info@jaxio.com
 ! Follow us on twitter: @springfuse
 ! Template pack-jsf2-primefaces:src/main/webapp/WEB-INF/flows/selectFlow.e.vm.xml
-->
<flow xmlns="http://www.springframework.org/schema/webflow" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd"
	parent="abstract">
	<!-- note: this flow inherits from the 'abstract' flow -->

	<persistence-context />

	<var name="tdTagLazyDataModel" class="com.app.mpadmin.web.domain.TdTagLazyDataModel" />

	<decision-state id="selectChoice">
		<if test="create" then="edit" />
		<if test="sub" then="tdTagSubSelect" else="tdTagSelect" />
	</decision-state>

	<view-state id="tdTagSelect">
		<on-entry>
			<evaluate expression="messageUtil.displayDelayedMessages()" />
			<evaluate expression="persistenceContext.clear()" />
		</on-entry>
		<on-render>
			<evaluate expression="flowsMenuHandler.updateMenu(flowRequestContext,'select')" />
		</on-render>
		<transition on="select" to="checkIfAlreadyOpenFlow" validate="false"/>
		<transition on="delete" validate="false">
			<evaluate expression="tdTagController.delete(tdTagLazyDataModel.selectedRow)" />
		</transition>
		<transition on="sendNew" to="edit" validate="false" />
        <transition on="updateAll">
            <evaluate expression="tdTagController.updateAll(tdTagLazyDataModel.selectedList)"/>
            <evaluate expression="primeFacesUtil.refresh()"/>
        </transition>
        <transition on="deleteAll">
            <evaluate expression="tdTagController.deleteAll(tdTagLazyDataModel.selectedList)"/>
            <evaluate expression="primeFacesUtil.refresh()"/>
        </transition>
	</view-state>

	<view-state id="tdTagSubSelect" view="tdTagSelect.xhtml">
		<transition on="select" to="tdTagSelected" validate="false"/>
	</view-state>

	<decision-state id="checkIfAlreadyOpenFlow">
		<if test="flowsMenuHandler.isFlowAlreadyOpen(flowRequestContext,tdTagLazyDataModel.selectedRow)" 
			then="endSwitchToAlreadyOpenFlow"
			else="edit" />
	</decision-state>

	<subflow-state id="edit" subflow="tdTag/edit">
		<input name="tdTag" value="tdTagLazyDataModel.selectedRow" />
		<transition on="endEdit" to="quitChoice" />
		<transition on="deleteAndClose" to="tdTagSelect" />
	</subflow-state>

	<decision-state id="quitChoice">
		<if test="create" then="quit" else="tdTagSelect"/>
	</decision-state>

	<end-state id="endSwitchToAlreadyOpenFlow"
		view="externalRedirect:contextRelative:#{flowsMenuHandler.endStateRedirectToAlreadyOpenFlow(flowRequestContext,tdTagLazyDataModel.selectedRow)}" />

	<end-state id="tdTagSelected">
		<output name="tdTagSelected" value="tdTagLazyDataModel.selectedRow" />
	</end-state>

	<global-transitions>
		<transition on="search">
			<evaluate expression="tdTagLazyDataModel.onSearch()" />
		</transition>
	</global-transitions>
</flow>