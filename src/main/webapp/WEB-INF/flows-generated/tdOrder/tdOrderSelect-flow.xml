<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 ! Source code generated by Celerio, a Jaxio product
 ! Want to use Celerio within your company? email us at info@jaxio.com
 ! Follow us on twitter: @springfuse
 ! Template pack-jsf2-primefaces:src/main/webapp/WEB-INF/flows/selectFlow.e.vm.xml
-->
<flow xmlns="http://www.springframework.org/schema/webflow" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd"
	parent="abstract">
	<!-- note: this flow inherits from the 'abstract' flow -->

	<persistence-context />

	<var name="tdOrderLazyDataModel" class="com.app.mpadmin.web.domain.TdOrderLazyDataModel" />

	<decision-state id="selectChoice">
		<if test="create" then="edit" />
		<if test="sub" then="tdOrderSubSelect" else="tdOrderSelect" />
	</decision-state>

	<view-state id="tdOrderSelect">
		<on-entry>
			<evaluate expression="messageUtil.displayDelayedMessages()" />
			<evaluate expression="persistenceContext.clear()" />
		</on-entry>
		<on-render>
			<evaluate expression="flowsMenuHandler.updateMenu(flowRequestContext,'select')" />
		</on-render>
		<transition on="select" to="checkIfAlreadyOpenFlow" validate="false"/>
		<transition on="delete" validate="false">
			<evaluate expression="tdOrderController.delete(tdOrderLazyDataModel.selectedRow)" />
		</transition>
		<transition on="sendNew" to="edit" validate="false" />
        <transition on="updateAll">
            <evaluate expression="tdOrderController.updateAll(tdOrderLazyDataModel.selectedList)"/>
            <evaluate expression="primeFacesUtil.refresh()"/>
        </transition>
        <transition on="deleteAll">
            <evaluate expression="tdOrderController.deleteAll(tdOrderLazyDataModel.selectedList)"/>
            <evaluate expression="primeFacesUtil.refresh()"/>
        </transition>
	</view-state>

	<view-state id="tdOrderSubSelect" view="tdOrderSelect.xhtml">
		<transition on="select" to="tdOrderSelected" validate="false"/>
	</view-state>

	<decision-state id="checkIfAlreadyOpenFlow">
		<if test="flowsMenuHandler.isFlowAlreadyOpen(flowRequestContext,tdOrderLazyDataModel.selectedRow)" 
			then="endSwitchToAlreadyOpenFlow"
			else="edit" />
	</decision-state>

	<subflow-state id="edit" subflow="tdOrder/edit">
		<input name="tdOrder" value="tdOrderLazyDataModel.selectedRow" />
		<transition on="endEdit" to="quitChoice" />
		<transition on="deleteAndClose" to="tdOrderSelect" />
	</subflow-state>

	<decision-state id="quitChoice">
		<if test="create" then="quit" else="tdOrderSelect"/>
	</decision-state>

	<end-state id="endSwitchToAlreadyOpenFlow"
		view="externalRedirect:contextRelative:#{flowsMenuHandler.endStateRedirectToAlreadyOpenFlow(flowRequestContext,tdOrderLazyDataModel.selectedRow)}" />

	<end-state id="tdOrderSelected">
		<output name="tdOrderSelected" value="tdOrderLazyDataModel.selectedRow" />
	</end-state>

	<global-transitions>
		<transition on="search">
			<evaluate expression="tdOrderLazyDataModel.onSearch()" />
		</transition>
	</global-transitions>
</flow>